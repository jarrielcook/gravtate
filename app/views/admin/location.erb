Report for location: [<%= @location.lat %>, <%= @location.lon %>]
<a href="http://maps.google.com/maps?q=
<% if(@location.lat < 0) %>
+
<% end %>
<%= @location.lat %>,
<% if(@location.lon < 0) %>
+
<% end %>
<%= @location.lon %>" target="_blank">map</a>
<br/><br/><br/>

<table border="1">

<tr>
<th>Tag</th>
<th># Users</th>
<th>Created</th>
</tr>

<% @location.references.each do |r| -%>

<tr>
<td><%= link_to r.tag.tag, :controller => :admin, :action => :tag, :id => r.tag.id %></td>
<td><%= link_to_if (r.users.length > 0), r.users.length, :controller => :admin, :action => :users_for_location, :id => @location.id %></td>
<td><%= r.created_at %></td>
</tr>

<% end -%>

</table>

<br/><br/><br/>
<%= link_to 'All Locations', :controller => :admin, :action => :locations %>
